spring:
  application:
    name: fill-dummy-value

logging:
  pattern:
    console: "%clr(%d{yyyy-MM-dd}){faint} %clr(%d{HH:mm:ss.SSS}){yellow} %clr([${LOG_LEVEL_PATTERN:%5p}]) %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:%wEx}"